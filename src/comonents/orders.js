import React from 'react';

const Orders = (props) => {
    const { orders } = props
    return(
        <div className='orders-container'>
            {}
        </div>
    )
}

export default Orders
